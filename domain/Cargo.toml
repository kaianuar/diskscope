[package]
name = "domain"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
description = "DiskScope domain layer: pure-Rust entities and ports, zero runtime dependencies."

[lib]
name = "domain"
path = "src/lib.rs"

[features]
# Sync-domain types (SyncEvent, SyncError, SyncTransport port).
# Always safe to enable — no extra dependencies.
sync = []

[lints]
workspace = true
