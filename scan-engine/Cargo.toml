[package]
name = "scan-engine"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
description = "DiskScope scan engine adapter. Placeholder for Phase 1; concrete implementation lands in Phase 2."

[lib]
name = "scan_engine"
path = "src/lib.rs"

[dependencies]
domain = { path = "../domain" }

[lints]
workspace = true