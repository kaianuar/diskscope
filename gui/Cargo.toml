[package]
name = "diskscope-gui"
version.workspace = true
edition.workspace = true
license.workspace = true

[[bin]]
name = "diskscope-gui"
path = "src/main.rs"

[dependencies]
scan-engine = { path = "../scan-engine" }
