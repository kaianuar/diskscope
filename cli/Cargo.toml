[package]
name = "diskscope-cli"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
description = "DiskScope CLI. Placeholder for Phase 1; real implementation (clap) in Phase 3."

[[bin]]
name = "diskscope"
path = "src/main.rs"

[dependencies]
domain = { path = "../domain" }
scan-engine = { path = "../scan-engine" }

[lints]
workspace = true
